# Local development LiveKit configuration. API keys and optional webhook
# delivery are injected through scripts/start-livekit.sh from environment.
port: 7880
log_level: info

rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false
